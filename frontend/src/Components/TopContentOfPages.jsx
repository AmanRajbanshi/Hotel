import React from "react";
import TopContentPageCard from "./TopContentPageCard";

const TopContentOfPages = ({ title, subtitle }) => {
  return (
    <>
      <TopContentPageCard title={title} subtitle={subtitle} />
    </>
  );
};

export default TopContentOfPages;
